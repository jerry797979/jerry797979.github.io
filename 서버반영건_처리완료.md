# 서버 반영건 3개 — 처리 완료

보내주신 요청 3건 모두 저장소에 반영했습니다.
지적해 주신 내용은 전부 사실이었고, 원인 분석도 정확하셨습니다.

**반영 커밋** `48abf35`

이제 서버에서 `git pull` 만 하시면 됩니다.
**손으로 되돌렸다 다시 적용하시던 파일 3개, 이제 건드리실 필요 없습니다.**

```bash
cd /var/www/ziotes
git checkout -- _tools/gen_solution.py dist/index.html dist/_router.php
git pull
sudo chown -R www-data:www-data dist
```

첫 줄은 서버에 남아 있는 로컬 수정을 먼저 지우는 것입니다.
그대로 두면 `git pull` 이 충돌로 막힙니다.

---

## 1. 홈 푸터 메뉴에 '정보' 추가 — 완료

`dist/index.html` 푸터 `<div class="fnav">` 에 넣었습니다.

```html
<a href="glossary/">용어집</a><a href="posts/">정보</a><a href="contact/">상담문의</a>
```

말씀하신 원인이 맞습니다. 홈은 `_tools/*.py` 가 만드는 파일이 아니라
지난 푸터 작업에서 홀로 빠졌습니다. 2차 문서의 "하단 메뉴에 정보를 넣었습니다"는
생성 대상 43장에만 해당됐던 것이고, 홈은 누락이었습니다. 바로잡았습니다.

---

## 2·3. og:image 도메인 — 완료

두 파일 모두 `https://ziotes.com/assets/og.png` 로 바꿨습니다.

- `dist/index.html` 18번째 줄
- `dist/_router.php` 155번째 줄

제안해 주신 대로 `_tools/gen_solution.py` 의 `OG_BASE` 기본값도
`https://ziotes.com` 으로 바꿨습니다.

```python
OG_BASE = "https://ziotes.com"
```

**이 값은 og:image 에만 쓰입니다.** 사이트맵과 canonical 은 별도의 `SITE` 값을
쓰는데 그쪽은 이미 `https://ziotes.com` 이었습니다. 그래서 이번 변경으로
검색 노출 쪽에 영향이 가는 곳은 없습니다.

`_config.php` 로 빼는 방식도 제안해 주셨는데, 이번에는 값을 하나로 고정하는
쪽을 택했습니다. 서비스 주소가 ziotes.com 하나로 정해진 이상 갈라 둘 이유가
없고, 파일이 늘면 관리할 것도 같이 늘기 때문입니다. 앞으로 주소가 또 바뀌거나
스테이징 주소가 따로 필요해지면 그때 말씀 주시면 상수로 빼겠습니다.

---

## 확인 결과

알려주신 명령 두 개를 그대로 돌렸습니다.

```
grep -rl "jerry797979" dist/        →  0개   (기대: 결과 없음)
grep -c 'href="posts/"' dist/index.html  →  3    (기대: 3 이상)
```

추가로 확인한 것입니다.

- **HTML 44장 전체의 og:image** — 44장 모두 `https://ziotes.com/assets/og.png`
  (다른 값이 섞인 곳 0건)
- `php -l dist/_router.php` — 문법 오류 없음
- 내부 링크 2,714개 — 끊긴 링크 0개
- 배포 후 실제 홈에 접속해 og:image 와 푸터 '정보' 링크 확인 완료

---

## 반영 후 살펴봐 주실 것

카카오톡 미리보기 그림은 카톡 쪽에 한동안 저장되어 있습니다.
바꾼 주소가 바로 안 보일 수 있는데, 시간이 지나면 갱신됩니다.

급히 확인하시려면 카카오 디벨로퍼스의 캐시 초기화 도구를 쓰시면 됩니다.

**→ https://developers.kakao.com/tool/clear/og**

---

문의사항 있으시면 언제든 연락 주십시오.
