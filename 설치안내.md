# 지오테스 홈페이지 인수인계

㈜지오테스솔루션 홈페이지 소스와 서버 설치·운영 방법입니다.

| 항목 | 내용 |
|---|---|
| **소스 저장소** | https://github.com/jerry797979/jerry797979.github.io |
| **서비스 도메인** | https://ziotes.com |
| **서버 환경** | Ubuntu + nginx + PHP-FPM |
| **페이지 수** | 일반 44개 + 지역 페이지 53,238개 |
| **기술** | 정적 HTML + CSS + PHP. 프레임워크·빌드도구·의존성 없음 |

---

## 1. 어떻게 만들어져 있나

리액트 같은 프레임워크를 쓰지 않았습니다. **파일을 그대로 올리면 뜨는** 구조라 가볍고 빠릅니다. `npm install` 같은 과정이 없습니다.

```
dist/                      ← nginx가 바라볼 폴더 (웹 루트)
  index.html               홈
  assets/nova.css          랜딩형 페이지 스타일
  assets/nova-post.css     문서형 페이지 스타일
  solution/                솔루션 상세 9개
  posts/                   정보 (블로그 글이 쌓이는 곳)
  guide/ glossary/         가이드·용어집
  cases/ about/ contact/   구축사례·회사소개·상담문의
  _lead.php                상담 신청 접수
  _leads.php               상담 신청 목록 (관리자)
  _router.php              지역 페이지 53,238장을 즉석에서 생성
  _data/                   지역 데이터 (외부 공개 금지)
_tools/                    페이지를 만들어내는 파이썬 스크립트
```

### 중요 — HTML을 직접 고치지 마세요

`dist/` 안의 HTML 대부분은 손으로 쓴 것이 아니라 **파이썬 스크립트가 찍어낸 결과물**입니다. HTML을 직접 고치면 당장은 반영되지만, 나중에 스크립트를 다시 돌리는 순간 **수정한 내용이 사라집니다.**

| 고치고 싶은 곳 | 고쳐야 할 파일 |
|---|---|
| 솔루션 페이지 9개 | `_tools/gen_solution.py` |
| 구축사례·회사소개·상담문의·화면예시·활용사례·업종별 | `_tools/gen_pages.py` |
| 가이드·용어집 | `_tools/gen_guide.py` |
| 정보(블로그 글) | `_tools/gen_posts.py` |
| 화면 예시 그림 조각 (여러 페이지 공용) | `_tools/mocks.py` |
| 상단 메뉴·바닥글 (전 페이지 공통) | `_tools/gen_solution.py` 의 `header()` |

고친 뒤 해당 스크립트를 실행하면 HTML이 새로 만들어집니다.

> **예외** — `dist/index.html`(홈)과 `dist/_router.php`(지역 페이지)는 스크립트가 건드리지 않으므로 직접 고치셔도 됩니다.

---

## 2. 서버 설치

### 2-1. 필요한 것 설치

```bash
sudo apt update
sudo apt install -y nginx php-fpm git python3
```

PHP 버전을 확인해 두세요. nginx 설정에 그대로 적어야 합니다.

```bash
ls /run/php/
# php8.1-fpm.sock 처럼 나옵니다 — 이 이름을 기억하세요
```

### 2-2. 소스 내려받기

```bash
sudo mkdir -p /var/www
cd /var/www
sudo git clone https://github.com/jerry797979/jerry797979.github.io.git ziotes
sudo chown -R www-data:www-data /var/www/ziotes
```

이제 웹 루트는 **`/var/www/ziotes/dist`** 입니다. (`dist` 안쪽이 웹 루트라는 점에 주의하세요.)

### 2-3. 상담 신청 저장 폴더 권한

상담 신청 데이터는 **웹 루트 바깥**(`/var/www/ziotes/_ziotes_leads`)에 쌓입니다. 주소로 직접 열리면 안 되는 개인정보이기 때문입니다.

```bash
sudo mkdir -p /var/www/ziotes/_ziotes_leads
sudo chown -R www-data:www-data /var/www/ziotes/_ziotes_leads
sudo chmod 750 /var/www/ziotes/_ziotes_leads
```

### 2-4. nginx 설정

> ⚠️ **가장 중요한 부분입니다.** 원래 소스에는 Apache용 `.htaccess` 로 보안 설정이 되어 있는데, **nginx는 `.htaccess` 를 완전히 무시합니다.** 아래 설정을 넣지 않으면 **상담 신청자 개인정보 파일이 주소만 알면 그대로 열립니다.**

`/etc/nginx/sites-available/ziotes.com` 파일을 만듭니다.

```bash
sudo nano /etc/nginx/sites-available/ziotes.com
```

```nginx
server {
    listen 80;
    listen [::]:80;
    server_name ziotes.com www.ziotes.com;

    root /var/www/ziotes/dist;
    index index.html index.php;
    charset utf-8;

    # ── 지역 페이지 53,238장 ──────────────────────────────
    # /local/ 로 오는 주소는 전부 _router.php 가 즉석에서 만들어 냅니다.
    location /local/ {
        try_files $uri /_router.php;
    }

    # ── 외부에 열리면 안 되는 것 (.htaccess 대체) ──────────
    location ^~ /_data/  { deny all; }   # 지역 데이터
    location ^~ /_leads/ { deny all; }   # 상담 신청 원본
    location = /_config.php         { deny all; }   # 관리자 열쇠·토큰
    location = /_config.example.php { deny all; }

    # ── PHP 실행 ─────────────────────────────────────────
    location ~ \.php$ {
        include snippets/fastcgi-php.conf;
        fastcgi_pass unix:/run/php/php8.1-fpm.sock;   # ← 2-1에서 확인한 이름으로
    }

    # ── 정적 파일 캐시 ───────────────────────────────────
    location ~* \.(css|js|png|jpg|jpeg|webp|svg|ico|woff2?)$ {
        expires 7d;
        add_header Cache-Control "public";
        access_log off;
    }

    # ── 나머지 ───────────────────────────────────────────
    location / {
        try_files $uri $uri/ =404;
    }

    gzip on;
    gzip_types text/css application/javascript application/xml image/svg+xml;
    gzip_min_length 1024;
}
```

설정을 켜고 문법을 검사한 뒤 반영합니다.

```bash
sudo ln -s /etc/nginx/sites-available/ziotes.com /etc/nginx/sites-enabled/
sudo nginx -t          # syntax is ok 가 떠야 합니다
sudo systemctl reload nginx
```

### 2-5. HTTPS 인증서

```bash
sudo apt install -y certbot python3-certbot-nginx
sudo certbot --nginx -d ziotes.com -d www.ziotes.com
```

certbot이 위 설정 파일에 인증서 항목과 80→443 전환을 **자동으로 넣어 줍니다.** 직접 고치실 필요 없습니다. 갱신도 자동입니다.

### 2-6. 상담폼 설정

```bash
cd /var/www/ziotes/dist
sudo cp _config.example.php _config.php
sudo nano _config.php
sudo chown www-data:www-data _config.php
sudo chmod 640 _config.php
```

| 항목 | 용도 |
|---|---|
| `admin_key` | 관리자 목록 접속 열쇠. **비워두면 목록 화면이 열리지 않습니다** |
| `telegram_token`, `telegram_chat_id` | 신청이 들어오면 텔레그램 알림 (선택) |
| `mail_to` | 신청이 들어오면 이메일 알림 (선택) |
| `store_dir` | 저장 위치. `null` 로 두면 2-3에서 만든 폴더를 씁니다 |
| `min_seconds`, `per_ip_limit` | 스팸 차단 |

관리자 화면 접속:

```
https://ziotes.com/_leads.php?key=지정한열쇠값
```

> ⚠️ `_config.php` 는 반드시 **UTF-8 (BOM 없음)** 으로 저장하세요. 앞에 보이지 않는 문자가 붙으면 `headers already sent` 오류로 **상담 접수가 통째로 실패합니다.** 리눅스에서 `nano`·`vi` 로 편집하시면 문제없습니다.

---

## 3. 도메인에 맞게 주소 설정 바꾸기

`_tools/gen_solution.py` 파일 맨 위 두 값을 확인하세요.

```python
SITE    = "https://ziotes.com"              # 그대로 두시면 됩니다
OG_BASE = "https://jerry797979.github.io"   # ← 이 값을 바꿔야 합니다
```

`OG_BASE` 는 카톡·문자로 링크를 보낼 때 뜨는 **썸네일 이미지 주소**입니다. 실제로 열리는 주소여야 썸네일이 나옵니다.

```bash
cd /var/www/ziotes
sudo sed -i 's|OG_BASE = "https://jerry797979.github.io"|OG_BASE = "https://ziotes.com"|' _tools/gen_solution.py
```

바꾼 뒤 페이지를 다시 만듭니다.

```bash
python3 _tools/gen_solution.py
python3 _tools/gen_pages.py
python3 _tools/gen_guide.py
python3 _tools/gen_posts.py
python3 _tools/gen_sitemap.py
sudo chown -R www-data:www-data dist
```

---

## 4. 설치 후 확인

| 확인할 것 | 방법 | 정상 결과 |
|---|---|---|
| 홈 | https://ziotes.com | 화면이 뜸 |
| 지역 페이지 (허브) | https://ziotes.com/local/seoul/gangnam/ | 강남구 페이지가 뜸 |
| 지역 페이지 (업종) | https://ziotes.com/local/seoul/gangnam/hospital/ | 강남구 병원 페이지가 뜸 |
| 상담 신청 | 상담문의 페이지에서 실제로 신청 | "접수되었습니다" 표시 |
| 관리자 목록 | `/_leads.php?key=열쇠값` | 방금 넣은 신청이 보임 |
| **데이터 차단** | https://ziotes.com/_leads/ | **403 이어야 함** |
| **데이터 차단** | https://ziotes.com/_data/topics.json | **403 이어야 함** |
| **설정 차단** | https://ziotes.com/_config.php | **403 이어야 함** |

굵게 표시한 세 줄은 **개인정보·비밀값 보호와 직결**되므로 반드시 확인하세요. 403이 아니라 내용이 보이면 nginx 설정이 제대로 안 들어간 것입니다.

내부 링크가 깨졌는지 한 번에 검사할 수도 있습니다.

```bash
python3 _tools/check_links.py
```

---

## 5. 정보(블로그) 글 올리기

`_tools/gen_posts.py` 안의 `POSTS` 목록에 항목을 추가하고 실행하면 끝입니다.

```python
{
 "slug": "call-center-cost",     # 주소가 됩니다 (영문 소문자·하이픈)
 "cat": "비용",                   # 목록 카드의 꼬리표
 "title": "글 제목",
 "desc": "목록에 보이는 두세 줄 설명",
 "h1": "화면에 크게 나올 제목",
 "sub": "제목 아래 한두 줄",
 "answer_q": "이 글이 답하는 질문",
 "answer": "맨 위 결론 박스 내용",
 "body": [ ... ],                 # 본문 (기존 글 참고)
 "faq": [ ("질문", "답변"), ... ],
}
```

```bash
python3 _tools/gen_posts.py
sudo chown -R www-data:www-data dist/posts
```

글마다 맨 위에 **"한 줄 결론" 박스**를 두는 구조입니다. AI 검색(제미나이·퍼플렉시티 등)이 이 문단을 인용해 가도록 만든 것이라 새 글에도 넣어주세요.

---

## 6. 소스 업데이트 받기

```bash
cd /var/www/ziotes
sudo git pull
sudo chown -R www-data:www-data dist
```

`_config.php` 와 `_ziotes_leads/` 는 저장소에 없으므로 `git pull` 로 덮어써지지 않습니다.

---

## 7. 저장소에 포함하지 않은 것

보안·용량 문제로 제외했습니다.

- `dist/_config.php` — 관리자 열쇠와 알림 토큰
- `_ziotes_leads/`, `dist/_leads/*.jsonl` — 수집된 상담 신청 (개인정보)
- `_notes/` — 내부 기획 문서
- 배경 이미지 원본 PNG — 웹에는 용량을 줄인 `dist/assets/*.webp` 를 씁니다

---

## 8. 참고

- **홈 메인 배경**은 접속할 때마다 4종 중 하나가 무작위로 표시됩니다 (`dist/index.html` 하단 스크립트)
- **사이트 색상**은 `dist/assets/nova.css` 맨 위 `:root` 에 모여 있습니다 (보라 `#6d4aff`, 민트 `#35e0a1`)
- **카톡 썸네일 이미지** 재생성: `python3 _tools/gen_og.py`
- **지역 페이지 주소 구조**
  ```
  /local/{시도}/                             시도 허브            17
  /local/{시도}/{시군구}/                     시군구 허브         229
  /local/{시도}/{시군구}/{업종}/               시군구 × 업종     7,557
  /local/{시도}/{시군구}/{읍면동}/             읍면동 허브       3,495
  /local/{시도}/{시군구}/{읍면동}/{서비스}/     읍면동 × 서비스  41,940
  ```
  HTML을 미리 만들어 두지 않고 접속할 때 즉석에서 만듭니다. 그래서 문구 한 줄을 고치면 5만 장에 한 번에 반영됩니다. 고치실 곳은 `dist/_router.php` 입니다.

문의사항은 담당자에게 연락 주시기 바랍니다.
