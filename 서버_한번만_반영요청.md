# 서버 반영 요청 — 한 번만 부탁드립니다

정보 글(`/posts/`) 안에 **상담 신청 폼**을 직접 넣었습니다.
글을 읽다 문의하고 싶어진 분이 다른 페이지로 옮기지 않고 그 자리에서 신청할 수 있습니다.

그런데 이 변경은 **글 파일만으로는 안 됩니다.** 스타일(CSS)과 스크립트(JS)가 같이 가야 합니다.
지금 `_post.php` 는 글만 받도록 되어 있어 저희가 보낼 수 없습니다.

그래서 **이번 한 번만** 반영을 부탁드립니다.
이후로는 스타일·스크립트까지 저희가 보낼 수 있어 다시 부탁드릴 일이 없습니다.

---

## 해 주실 것

```bash
cd /var/www/ziotes
git fetch origin
git reset --hard origin/main
sudo chown -R www-data:www-data dist
```

`git pull` 대신 `git fetch` + `git reset --hard` 를 적어 드린 이유가 있습니다.
아래 "서버가 저장소보다 앞서 있던 부분"을 봐 주십시오.

끝나면 아래로 확인해 주십시오.

```bash
curl -s -H "X-Post-Key: 넣으신열쇠" https://ziotes.com/_post.php | head -c 200
```

`{"ok":true,...}` 가 나오면 정상입니다.

---

## 서버가 저장소보다 앞서 있던 부분 — 저희가 맞췄습니다

작업 중에 **라이브 사이트에는 있는데 저희 저장소에는 없는 것**을 발견했습니다.

상담 폼의 **이메일 칸**입니다. 홈과 상담문의 페이지 양쪽에 들어가 있었는데
저장소에는 없었습니다. 모르고 그대로 재생성했다면 **이메일 칸이 지워질 뻔했습니다.**

라이브에서 그대로 가져와 저장소에 되살렸습니다. 이번 반영으로 사라지지 않습니다.

### 그러면서 하나 더 나왔습니다

`_lead.php` 가 **이메일을 아예 받지 않고 있었습니다.**
폼에는 칸이 있는데 서버가 그 값을 읽지 않아, 지금까지 손님이 적어 주신 이메일은
저장도 알림도 되지 않고 그냥 버려지고 있었습니다.

이번에 고쳤습니다.

- 접수 파일에 `email` 이 함께 저장됩니다
- 텔레그램·메일 알림에 이메일 줄이 들어갑니다
- 관리자 목록(`/_leads.php`)에 **이메일** 칸이 생기고 눌러서 메일을 보낼 수 있습니다
- 적었는데 형식이 틀린 경우만 걸러냅니다. 비워 두면 그대로 접수됩니다

지금까지 들어온 접수 기록에는 이메일이 없습니다. 되살릴 수 없는 부분입니다.

> 혹시 서버에서 `_lead.php` 를 이미 고쳐 두셨다면 알려 주십시오.
> `git reset --hard` 는 그 수정을 저희 것으로 덮어씁니다.
> 고치신 게 있다면 반영 전에 맞춰 두겠습니다.

---

## 이번에 바뀌는 것

**글 페이지 (`/posts/`, `/guide/`, `/glossary/`)**

- 자주 묻는 것 아래에 **무료 상담 신청 폼**이 들어갑니다
- 맨 아래 보라색 상자의 **"무료 상담신청"** 버튼은 상담문의 페이지로 보내지 않고
  같은 글 안의 그 폼으로 내려갑니다
- 버튼 글씨를 **"상담 신청" → "무료 상담신청"** 으로 바꿨습니다.
  상담이 무료라는 것이 버튼에서 보이지 않았습니다

**상담 폼 (홈·상담문의·글 페이지)**

- 이메일 칸이 정식으로 동작합니다

**`_post.php`**

- 글에 더해 **`assets/` 안의 CSS·JS 파일**도 받을 수 있게 했습니다
- 받는 조건이 좁습니다. **이미 서버에 있는 파일을 같은 이름으로 바꾸는 것만** 됩니다
  - 새 파일 만들기 — 거부
  - `assets/` 밖의 경로 — 거부
  - `.css` · `.js` 가 아닌 확장자 — 거부
  - `../` 로 위 폴더 올라가기 — 거부
- 저희 쪽에서 직접 두드려 확인했습니다. 위 네 가지 모두 400 으로 막힙니다

---

## 왜 assets 까지 여는지

지금 구조로는 저희가 글의 **내용**은 바꿀 수 있지만 **모양**은 못 바꿉니다.
버튼 하나 옮기는 일에도 서버에서 `git pull` 을 눌러 주셔야 했습니다.

`assets` 를 열어 두면 그 부탁이 없어집니다.
대신 아무 파일이나 심을 수 없도록, **이미 있는 CSS·JS 를 같은 이름으로 갈아 끼우는 것**
한 가지로만 좁혔습니다.

`.php` 는 어떤 경우에도 받지 않습니다. 이 통로로 서버에서 코드가 실행되는 일은 없습니다.

---

## 반영 후

저희가 스타일·스크립트를 보내고 글을 다시 올린 뒤,
실제로 폼이 뜨고 접수까지 되는지 확인해서 알려 드리겠습니다.

문의사항 있으시면 연락 주시기 바랍니다.
